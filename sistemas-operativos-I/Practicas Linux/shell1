if [ $# -ne 1 ]
then
echo "Vaya mierda de parametros"
exit
fi

if [ -f $1 ]
then
	if [ -x $1 ]
	then 
	echo "Tiene permisos de ejecucion"	
	else echo "No tiene permisos de ejecucion"
	fi
else echo "No se ha pasado un fichero regular"
exit
fi


